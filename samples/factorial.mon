

let factorial = fn(x) {
    if (x == 0) {
        return 1;
    }
    return x * factorial(x - 1);
};

puts("calculate n factorial(n!) :");
puts("---------------------------");

let n = input("enter n: ");
puts("result =", factorial(n));